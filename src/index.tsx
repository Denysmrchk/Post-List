import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Путь к вашему основному компоненту приложения

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root'),
);
